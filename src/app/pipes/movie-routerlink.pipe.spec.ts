import { MovieRouterlinkPipe } from './movie-routerlink.pipe';

describe('MovieRouterlinkPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieRouterlinkPipe();
    expect(pipe).toBeTruthy();
  });
});
