import { MultilineEllipsisDirective } from './multiline-ellipsis.directive';

describe('ClampDirective', () => {
  it('should create an instance', () => {
    const directive = new MultilineEllipsisDirective();
    expect(directive).toBeTruthy();
  });
});
