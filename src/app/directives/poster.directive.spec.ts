import { PosterDirective } from './poster.directive';

describe('PosterDirective', () => {
  it('should create an instance', () => {
    const directive = new PosterDirective();
    expect(directive).toBeTruthy();
  });
});
